Flag Match World v5 - Startup/Button Cache Fix
- Forces fresh page loads to Startup screen, not Pause.
- Removes stale pause modal state restored by iOS Safari/GitHub Pages.
- Adds cache-busting query strings for CSS/JS so deployed site loads the latest files.
- Rebinds startup buttons safely after initial UI reset.
